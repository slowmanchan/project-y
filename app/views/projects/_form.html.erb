<%= simple_form_for(project) do |f| %>
<%= f.input :title %>
<%= f.input :description %>
<%= f.input :level, label: "Difficulty Level", collection: 1..5  %>
<%= f.input :help, label: "Need help?" %>
<%= f.input :tag_list, label: "Technology" %>

<%= f.button :submit, class: "button" %>
<% end %>
