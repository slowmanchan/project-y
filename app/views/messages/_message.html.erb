<% user_status = message.user_id == current_user.id ? "reciever" : "sender" %>
<div class="<%= user_status %> callout">
  <p><%= message.body %></p>
  <p><span><%= message.user.email %> | <%= time_ago_in_words(message.created_at) %> ago</span></p>
</div>
