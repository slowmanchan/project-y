<%= render 'banner' %>
<div class="row">
  <div class="small-12 large-8 columns" id="meetings_calendar">
    <%= render "calendar", meetings: @meetings %>
  </div>
  <div class="meetings-index">
    <div class="small-12 large-4 columns">
      <%= render 'meetings' %>
    </div>
  </div>
  <%= render 'reveal' %>
</div>
